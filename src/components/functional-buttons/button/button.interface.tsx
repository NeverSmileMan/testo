import React, { ComponentType } from 'react';
import {SvgIconProps} from '@material-ui/core';

export interface ButtonProp {
    // nameButton: string;
    // buttonIcon: SvgIconProps;
    click: ()=>void,
    // backgroundColor?: string;
    // color?: string;
    // fontSize?: string;
}