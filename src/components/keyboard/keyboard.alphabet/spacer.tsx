import React from 'react';

export default function spacer() {
	return (
    // <div>{i < opts.keys[layout.name].length - 1 ? <div key={`${keys[0]}z`} className={classes.spacer} /> : null}</div>
    <></>
	);
}
