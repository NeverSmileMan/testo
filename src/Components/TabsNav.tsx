import React from 'react';

const style: React.CSSProperties = {
    top: '30px',
    left: '1px',
    height: '50px',
    width: '599px',
}

function TabsNav() {
    return (
        <div className='tabs-nav' style={style}>
            TABS NAVIGATION
        </div>
    );
}

export default TabsNav;
